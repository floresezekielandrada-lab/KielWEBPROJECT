This project no longer uses a separate side logo element.

If you want to update the site branding, replace the hero section logo image in `index.html` and keep the file name consistent with the `src` path.

Recommended formats: PNG (transparent background preferred), WebP, or JPG. Recommended size: around 800×800px (the site will scale it down to display).